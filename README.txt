Краткая актуальная сводка README

Основной источник правды: README.md.

Текущее состояние на 2026-08-10:
- серверная версия работает на `c576798a-ec5f-4491-b6a2-b28e74fc445f`;
- production URL серверной версии: `https://app-2657fd62db74.vibecode.bitrix24.tech`;
- Marketplace-версия находится в `marketplace/` и остаётся serverless, без backend;
- последний локальный Marketplace-архив для проверки: `Калькулятор счетов Marketplace v10-6.zip`;
- runtime/cache-busting версия: `layout-20260810-8`;
- Marketplace zip содержит только `install.html`, `install.js`, `install.css`, `index.html`, `app.js`, `style.css`.

Подтверждено пользователем:
- установщик завершается с `ok: true`;
- журнал установки накопительный и остаётся читаемым;
- создание полей работает;
- сохранение сопоставления возвращает `dealCard.ok=true`, `partial=false`;
- раздел `Расчёт оплаты счетов` и четыре поля появляются в карточке сделки;
- ручной пересчёт работает;
- даты в таблице выводятся без времени.
- в блок `Автопересчёт` добавлен режим `Ручной`, выбор окна, прогресс и ручной массовый пересчёт; CSV-отчёт формируется и скачивается только по кнопке.
- при выборе `Утром и вечером` или `Постоянный` показывается окно заявки на серверную версию, если у портала нет серверного флага в `app.option`.

Текущая проверка:
- проверить `Калькулятор счетов Marketplace v10-6.zip` на портале;
- особое внимание: названия стадий после исправления сборки `DT31_*:*` из `SMART_INVOICE_STAGE_*`;
- если стадия всё ещё отображается кодом, скопировать из журнала ручного пересчёта блок `stageLookup`.

Правила:
- перед изменениями выполнять `git status --short`;
- `public/menu.css` и визуальную композицию `public/index.html` не менять без отдельного подтверждения;
- старый сервер `e95ca529-f434-4432-bbb8-a7e0e8f85837` не использовать;
- Marketplace-архивы не перезаписывать, создавать только новые versioned zip;
- OAuth/API ключи не писать в документацию и репозиторий;
- push делать только после загрузки на портал, проверки пользователем и явного разрешения.
