Краткий промт для следующей сессии

Продолжаем проект Deal Invoice Summary.

Рабочая папка:
C:\AI Project B24\Deal Invoice Summary

GitHub:
https://github.com/DjjoniiL/Deal-Invoice-Summary
ветка main

Перед началом обязательно прочитать:
- README.md
- PROJECT_SPECIFICATION.md
- NEXT_SESSION.md
- DESIGN_GUIDE.md
- MARKETPLACE_MIGRATION_PLAN.md

Правила:
- перед любыми изменениями выполнить `git status --short`;
- не перетирать пользовательские изменения;
- `public/menu.css` и визуальную композицию `public/index.html` не менять без отдельного подтверждения;
- старый сервер `e95ca529-f434-4432-bbb8-a7e0e8f85837` не использовать;
- сервер `c576798a-ec5f-4491-b6a2-b28e74fc445f` не использовать для Marketplace-экспериментов: Marketplace-версия serverless;
- production URL серверной версии: `https://app-2657fd62db74.vibecode.bitrix24.tech`;
- для Marketplace создавать только новые versioned zip-архивы, старые архивы не перезаписывать;
- OAuth/API ключи не записывать в документацию и репозиторий;
- локальные коммиты обычно делать после каждого третьего существенного изменения;
- push делать только после загрузки правок на портал, проверки пользователем и явного разрешения.

Текущее состояние:
- серверная версия работает;
- Marketplace-адаптация находится в `marketplace/`;
- последний архив для ручной проверки: `Калькулятор счетов Marketplace v10-6.zip`;
- версия внутри архива: `layout-20260810-8`;
- установщик завершался с `ok: true`;
- сохранение сопоставления возвращало `dealCard.ok=true`, `partial=false`;
- раздел `Расчёт оплаты счетов` и поля появляются в карточке сделки;
- ручной пересчёт работает;
- даты в таблице читаемые, без времени;
- в блок `Автопересчёт` добавлен режим `Ручной`, выбор окна, прогресс, ручной массовый пересчёт всех сделок в выбранном окне и CSV-отчёт по кнопке после формирования;
- при выборе `Утром и вечером` или `Постоянный` показывается окно заявки на серверную версию, если у портала нет серверного флага в `app.option`;
- стадия проверяется после исправления сборки полного кода `DT31_*:*` из `SMART_INVOICE_STAGE_*`.

Следующий шаг:
1. Пользователь вручную загружает `Калькулятор счетов Marketplace v10-6.zip` на портал.
2. Проверить ручной пересчёт сделки ID 2, массовый пересчёт по выбранному окну, скачивание отчёта по кнопке и окна при выборе `Утром и вечером`/`Постоянный`.
3. Если стадия всё ещё кодом `DT31_2:N`, взять из журнала блок `stageLookup`.
4. После подтверждения пользователя можно готовить локальный commit; push только по отдельному разрешению.
