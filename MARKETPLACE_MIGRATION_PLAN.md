# План адаптации для Bitrix24 Marketplace

## Цель

Подготовить отдельный zip-архив статического приложения для загрузки в Bitrix24 Marketplace без собственной серверной части. Документация, тесты, исходная серверная версия и служебные файлы остаются в репозитории, но не попадают в Marketplace zip.

## Текущий статус

На 2026-08-31 serverless-адаптация подготовлена в папке `marketplace/` как Marketplace v36 с финальным архивом прав v37. Последний локальный архив для загрузки: `Расчёт оплат счетов Mini rights Final v.37.zip` (`layout-20260826-2`). Сборка остаётся static/serverless: v35 точечно убрала полный `BX24.reloadWindow()` из фонового worker и оставила только мягкий `reloadData`; v36 поверх этого добавляет мягкую проверку последней открытой сделки при возврате/нахождении пользователя на канбане. v37 фиксирует проверенный минимальный набор Marketplace-прав: `CRM`, `Встраивание приложений`, `Пользователи (минимальный) / user_brief`.

Что уже перенесено в Marketplace-версию:

- финальный дизайн основного экрана из серверной версии;
- пункт левого меню через `LEFT_MENU`;
- создание стандартных пользовательских полей сделки;
- отображение человекочитаемых названий полей вместо символьных кодов;
- сохранение сопоставления через `app.option.*`;
- попытка автоматического добавления раздела `Расчёт оплаты счетов` в карточку сделки при `Создать автоматически` и `Сохранить сопоставление`;
- настройка раздела карточки сделки по доступным воронкам через `crm.category.list` и `extras.dealCategoryId`, потому что в Bitrix24 раскладки карточек сделок зависят от воронки;
- fallback для ответа `Card layout is empty`: Marketplace-версия создаёт безопасную дефолтную раскладку и добавляет в неё раздел `Расчёт оплаты счетов`;
- ручной пересчёт по ID или ссылке сделки;
- человекочитаемые названия стадий счетов через `crm.status.list` по `SMART_INVOICE_STAGE_*` и `DYNAMIC_31_STAGE_*` для новых счетов Bitrix24; если `crm.status.list` возвращает полный `STATUS_ID` вида `DT31_2:S`, приложение использует его приоритетно вместо числового `ID` строки, если возвращает короткий `STATUS_ID`, собирает полный ключ из `ENTITY_ID`/`STATUS_ID` или из префикса запроса, а также диагностирует справочник через `crm.status.entity.types`;
- ФИО ответственных в таблице счетов через `user.get` и нормализацию ID пользователей; для этого нужен `Пользователи (базовый) / user_basic`, полный `Пользователи / user` не требуется;
- даты выставления и сроки оплаты в таблице и CSV-отчёте в формате `ДД.ММ.ГГГГ` без времени;
- накопительный установочный журнал с timestamp и 4-секундной паузой перед завершением установки;
- ручной массовый пересчёт сделок в выбранном окне `1 мес`/`3 мес`/`6 мес` с предварительным подтверждением, прогрессом и формированием CSV-отчёта за период;
- окно заявки на серверную версию при выборе режимов `Утром и вечером` или `Постоянный`; окно не показывается, если портал уже имеет серверный флаг в `app.option`;
- режим `При открытии сделки` выбран по умолчанию; ручной пересчёт по кнопке и массовый пересчёт доступны без серверного окна;
- поле `Расписание` в блоке `Автопересчёт`: `по кнопке` для ручного режима, `в 9:45 утра И в 18:45 вечера` для режима утром/вечером, `Каждый час с 09:00 до 20:00` для постоянного режима;
- автоматический возврат режима на `При открытии сделки` при закрытии окна заказа серверной версии без серверного флага;
- справочный расход micro-сервера в окне серверных режимов за 30 рабочих дней: до `200 руб/мес` для `Постоянный` и до `145 руб/мес` для `Утром и вечером`;
- CSV-отчёт в браузере по кнопке `Скачать отчёт` после формирования;
- cache-busting ассетов `layout-20260826-2`, расширенный журнал ошибок и статический `PAGE_BACKGROUND_WORKER`;
- режим `При открытии сделки`, который при загрузке вкладки карточки пересчитывает связанные счета и обновляет расчётные поля только если значения изменились;
- повторный пересчёт в открытой карточке при изменении суммы или стадии сделки;
- заголовок над таблицей с ID сделки, ссылкой на сделку и закреплённой суммой из карточки;
- порядок колонок счетов: `Счёт`, `Срок оплаты`, `Ответственный`, `Сумма`, `Стадия`, `Дата выставления`;
- поле остатка с названием `Остаток оплаты сделки`;
- режим `При изменении сделки/счёта`, который в Marketplace работает через `PAGE_BACKGROUND_WORKER` на открытых страницах портала.

- `layout-20260812-3`: монитор открытой карточки работает по сохранённым данным Bitrix24: polling не блокируется скрытым iframe, дополнительно запускается при `focus`, `pageshow`, `visibilitychange` и пробует подписаться на события, если они объявлены через `BX24.placement.getInterface`. Несохранённые правки в форме карточки без backend/документированного события остаются вне надёжного serverless-сценария.
- `layout-20260812-4`: добавлен статический `PAGE_BACKGROUND_WORKER` для открытых страниц портала. Worker берёт текущий URL из `BX24.placement.info()`/`PLACEMENT_OPTIONS.URI`, активируется на карточках сделок, проверяет сохранённые `OPPORTUNITY` и `STAGE_ID` раз в 5 секунд, пересчитывает поля через прямой Bitrix24 REST и после записи пробует обновить карточку через `reloadData`. Начиная с `layout-20260826-1`, если `reloadData` недоступен, worker не вызывает полный `BX24.reloadWindow`. Начиная с `layout-20260826-2`, worker запоминает последнюю открытую карточку сделки, заново читает активный URI на каждом цикле и при kanban URL приоритетно проверяет эту сделку через `crm.deal.get`. Режим `При изменении сделки/счёта` больше не требует внешней обработки в Marketplace-сценарии открытой страницы.
- `layout-20260812-5`: worker безопасно разбирает настройки `dealInvoiceSummarySettings` и возвращается к значениям по умолчанию, если Bitrix24 или `localStorage` вернули нестандартный/повреждённый формат.
- `layout-20260814-1`: Marketplace v19 создаёт стандартные расчётные поля и дефолтное сопоставление в установщике, сохраняет настройки для всех пользователей через `app.option`, добавляет раздел карточки сделки во все доступные воронки включая основную, закрывает блок сопоставления для не-администраторов через `user.admin` и не показывает символьные коды полей в выпадающих списках.
- `layout-20260814-2`: Marketplace v20 оставляет выбор воронки и периода доступным сотрудникам без админки, расширяет `PAGE_BACKGROUND_WORKER` на CRM-страницы/канбан через `crm.deal.list` по `DATE_MODIFY` для реакции на смену стадии сделки, убирает `placement.bind` для `LEFT_MENU` из установщика и правит нижнюю строку блока сопоставления.
- `layout-20260814-3`: Marketplace v21 сохраняет выбор воронки и периода как пользовательскую настройку `dealInvoiceSummaryUserCalculationSettings`, чтобы сотрудник без админки мог менять эти параметры без `app.option.set`; дефолт — все воронки и `1 мес`, список периодов — `1 мес`, `3 мес`, `6 мес`. Перед массовым расчётом добавлено окно `Запускаем расчёт?` с количеством сделок и оценкой времени с шагом 5 минут, а страница настроек получила устойчивую сетку действий.
- `layout-20260814-4`: Marketplace v22 записывает `0` в пустые расчётные поля сделки, если рассчитанное значение равно нулю; кнопка `Обратиться` открывает страницу настроек и автоматически разворачивает чат открытой линии; рядом с периодом массового расчёта добавлена справка о выборке сделок по `DATE_CREATE`/`BEGINDATE` и счетов по `createdTime`/`begindate`.
- `layout-20260814-5`: Marketplace v23 поджимает главную страницу: убрано растягивание карточек двухколоночной сетки по высоте, нижняя строка сопоставления полей стала компактной, пустое состояние блока массового расчёта центрируется до первого расчёта.
- `layout-20260814-6`: Marketplace v24 уточняет массовый расчёт: предварительная оценка времени округляется с шагом 30 секунд, во время пересчёта под прогресс-баром тикает `Время обработки` с шагом 5 секунд, а финальный статус фиксирует фактическое время обработки.
- `layout-20260814-7`: Marketplace v25 правит фронт главной страницы и статистику сделок: счётчик называется `Сделок в расчёте`, сообщение о правах стало короче, верхняя строка блока массового расчёта выровнена, окно статистики явно называется `Статистика сделок за период`, а успешные сделки учитываются по семантике стадии Bitrix24.
- `layout-20260817-1`: Marketplace v26 закрепляет верхнюю строку блока массового расчёта, добавляет в массовый CSV колонку `Стадия сделки` и выносит дополнительный учёт сделок прошлых периодов со счетами из выбранного периода в пользовательский чекбокс настроек. По умолчанию чекбокс включён.
- `layout-20260817-2`: Marketplace v27 добавляет в массовый CSV колонку `Тип воронки` и уточняет предварительную оценку времени массового расчёта до 10 секунд на сделку с округлением вверх до ближайших 30 секунд.
- `layout-20260817-3`: Marketplace v28 строго фильтрует массовый расчёт по выбранной воронке, включая сделки, найденные через счета периода; адаптирует главную страницу под узкий iframe и делает верхнюю кнопку обновления настоящей перезагрузкой iframe.
- `layout-20260817-4`: Marketplace v29 дополнительно фиксирует выезд элементов блока массового расчёта при 100% масштабе браузера.
- `layout-20260817-5`: Marketplace v30 восстанавливает сценарий `Обратиться` → настройки → автозапуск чата открытой линии и ограничивает обычные модальные окна видимой областью браузера.
- `layout-20260817-6`: Marketplace v31 делает модальное окно статистики сделок компактным без внутреннего скролла и добавляет открытие стандартного списка сделок Bitrix24 по клику на сектор диаграммы.
- `layout-20260817-7`: Marketplace v32 усиливает автозапуск чата открытой линии и делает ссылки секторов диаграммы раздельными: ID группы плюс semantic-фильтр для завершённых и активных сделок.
- `layout-20260817-8`: Marketplace v33 дополнительно повторяет API/DOM-попытки автозапуска чата открытой линии после первого сигнала и при событиях загрузки/видимости, увеличивает модальное окно статистики до `800px` без внутреннего скролла, добавляет конкретные `STAGE_ID` в ссылки секторов диаграммы и сохраняет `Последний запуск` массового расчёта в браузере между перезапусками iframe.
- `layout-20260817-9`: Marketplace v34 останавливает повторные попытки автозапуска чата после первого признака раскрытого виджета или успешного клика/API-вызова и увеличивает модальное окно статистики до `920px`, а диаграмму до `300px` без внутреннего скролла.
- `layout-20260826-1`: Marketplace v35 убирает полный `BX24.reloadWindow()` из фонового worker. После автоматической записи расчётных полей worker пробует только `reloadData`, а при недоступности команды не перезагружает окно Bitrix24.
- `layout-20260826-2`: Marketplace v36 добавляет мягкий сценарий возврата на канбан. Worker хранит последнюю открытую сделку в `dealInvoiceSummaryLastSeenDeal`, распознаёт kanban URL, раз в 15 секунд приоритетно проверяет эту сделку через `crm.deal.get` и при изменениях запускает `background-kanban-return-recalculate` без полной перезагрузки окна Bitrix24.

Финальный вывод v34: для открытой линии retry-логика без stop condition опасна. Рабочий serverless-паттерн: открыть `settings.html?openChat=1`, пробовать API виджета и DOM fallback, но сразу прекращать попытки после успешного API-вызова, клика по видимой кнопке или появления признаков раскрытого чата через `MutationObserver`.

Финальный вывод по диаграмме: кликабельные сектора должны передавать точный список ID сделок как основной фильтр. `STAGE_SEMANTIC_ID` и `STAGE_ID` полезны как дополнительные параметры, но стандартный UI Bitrix24 может не показать поле `Группа стадий`, поэтому нельзя строить критичный переход только на semantic-фильтре.
## Состав Marketplace zip

- `install.html` — обязательный установщик приложения.
- `install.js` — логика установки: накопительный журнал, очистка старых `LEFT_MENU`-привязок, `placement.bind` для `PAGE_BACKGROUND_WORKER`, затем `BX24.installFinish()`.
- `install.css` — стили установщика.
- `index.html` — основной экран приложения и вкладка сделки.
- `app.js` — расчёт, создание полей, сохранение настроек и вызовы REST через `BX24.callMethod`.
- `style.css` — стили приложения.
- `worker.html`, `worker.js`, `worker-error.html` — статический фоновый worker Bitrix24 для мониторинга сохранённых изменений открытой карточки сделки.

## Перенос логики

1. Заменить серверные VibeCode API-вызовы на прямые Bitrix24 REST-вызовы из iframe:
   - `crm.deal.get`;
   - `crm.deal.update`;
   - `crm.deal.fields`;
   - `crm.deal.userfield.list`;
   - `crm.deal.userfield.add`;
   - `crm.item.list` с `entityTypeId=31`;
   - `crm.status.list` для справочника стадий новых счетов `SMART_INVOICE_STAGE_*`;
   - `user.get` для ФИО ответственных, со скоупом `Пользователи (базовый) / user_basic`.
2. Хранить настройки маппинга через `app.option.*`, с fallback в `localStorage` для dev-установки.
3. Считать суммы только по текущему списку счетов, полученному из Bitrix24; удалённые счета не учитываются, потому что `crm.item.list` их не возвращает.
4. Оставить CSV-экспорт в браузере через `Blob`.

## Ограничения serverless-версии

Статический zip без сервера не может принимать входящие HTTP-события Bitrix24 как внешний обработчик. Для сценария уже открытой страницы используется официальный `PAGE_BACKGROUND_WORKER`: это не внешний backend, а скрытый iframe приложения, который Bitrix24 загружает на страницах портала. Он реагирует только пока у пользователя открыта страница портала и видит только сохранённые данные, доступные через REST.

## План serverless-доработки

Текущий Marketplace-этап реализовал режим `При открытии сделки`. Это почти автоматический режим без сервера: пользователь открывает карточку сделки, Bitrix24 загружает iframe приложения, `marketplace/app.js` получает ID сделки из `PLACEMENT_OPTIONS`, читает сделку и связанные счета через `BX24.callMethod`, считает суммы и обновляет поля сделки через `crm.deal.update`, если значения изменились.

Рабочие пункты:

1. Добавить режим `При открытии сделки` в список автопересчёта. Реализовано в `layout-20260812-1`.
2. При загрузке вкладки сделки запускать пересчёт с записью результата. Реализовано в `layout-20260812-1`.
3. Перед записью сравнивать текущие и рассчитанные значения, чтобы не обновлять сделку без необходимости. Реализовано в `layout-20260812-1`.
4. Убрать пункт `Ручной` из списка режимов, но оставить ручной пересчёт и массовый пересчёт по окну только по кнопке. Реализовано в `layout-20260812-2`.
5. В открытой карточке повторять пересчёт при изменении суммы или стадии сделки. Усилено через `PAGE_BACKGROUND_WORKER` в `layout-20260812-4`.
6. Режим `При изменении сделки/счёта` в Marketplace работает через `PAGE_BACKGROUND_WORKER` на открытых страницах портала.
7. Режимы `Утром и вечером` и `Постоянный` не запускать в serverless-версии и не подключать к Beget.
8. При возврате/нахождении пользователя на канбане приоритетно проверять последнюю открытую сделку без внешнего backend и без `BX24.reloadWindow()`. Реализовано в `layout-20260826-2`.

## Проверка перед упаковкой

1. `npm run lint`
2. `npm test`
3. При добавлении новой сложной функции в `marketplace/` добавить или обновить тест в `marketplace/marketplace-runtime.test.js` с описательным названием и комментарием, если проверка не очевидна.
4. Собрать новый Marketplace zip только из файлов папки `marketplace/`; для каждого крупного изменения использовать новое имя с номером версии или commit short SHA. Не обновлять старый безымянный zip и не собирать архив с серверной версией.
5. Установить архив на тестовом портале через developer console.
6. В developer console для версии приложения включить добавление основной страницы приложения в левое меню и указать название пункта меню `Расчёт оплаты счетов`. Это делается настройкой версии/описания приложения, а не `placement.bind`, чтобы Bitrix24 открывал приложение как основной iframe/слайдер.
7. Проверить, что `LEFT_MENU` появился после установки. Вкладку сделки настроить в developer console. Не вызывать `placement.bind` для `CRM_DEAL_DETAIL_TAB` из `install.js`: install token Marketplace-архива может не иметь нужных прав.
8. Проверить внешний вид: Marketplace-версия должна повторять утверждённый основной экран серверной версии, а не использовать отдельный упрощённый шаблон.
9. Проверить создание полей, сопоставление, вкладку сделки и ручной пересчёт.

## Граница Marketplace-only

Текущая задача решается только в статическом Marketplace zip без внешнего backend. Для реакции на сохранённые изменения открытой сделки используется `PAGE_BACKGROUND_WORKER`; для ручных сценариев остаются кнопка пересчёта одной сделки и массовый пересчёт по окну через Bitrix24 JS SDK/REST.

В рамках Marketplace-пакета не добавлять внешние обработчики, OAuth-секреты, API-ключи, Beget/VibeCode вызовы или архивы серверной версии. Любые дальнейшие эксперименты по сумме сделки сначала должны проверять официальные возможности Bitrix24, доступные из zip: placements, `BX24.placement.*`, `BX24.reloadWindow` и прямые REST-методы из iframe.
